# Moira — OPTIONAL extras only.
#
# The core engine has ZERO required runtime dependencies — pyproject.toml
# declares `dependencies = []`, and nothing under moira/ imports outside the
# standard library (plus the bundled native extension). This file mirrors the
# [project.optional-dependencies] lunar-graze extra for convenience:
#
#   pip install moira-astro[lunar-graze]
#
# (scipy was listed here historically as a runtime dependency, but it is
# imported nowhere in moira/ — the ΔT spline was fitted with scipy offline;
# the shipped model does not need it.)
spiceypy>=8.0
laspy[lazrs]>=2.7
requests>=2.31
