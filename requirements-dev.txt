# Moira — Development / test dependencies
# Keep in sync with pyproject.toml [project.optional-dependencies.dev]
-r requirements.txt

# Testing
pytest>=9.0
pytest-cov>=6.0
pytest-xdist>=3.5
pyyaml>=6.0

# Packaging / release checks
build>=1.2
twine>=6.0

# Accuracy validation (Horizons integration tests)
# (no extra deps — uses stdlib urllib)

# Astronomy oracle / standards cross-checks
pyerfa>=2.0
astropy>=6.0
scipy>=1.14

# Property-based testing
hypothesis>=6.0

# Optional: parallel test execution
# pytest-xdist>=3.0
